RewriteEngine On

# Sert les fichiers/dossiers reels tels quels (CSS, JS, images...)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Toutes les autres requetes passent par le front controller
RewriteRule ^ index.php [L]